[pytest]
asyncio_mode = auto
norecursedirs = tmp output data .venv .git scripts frontend node_modules worker .pytest_cache .pytest_tmp .superpowers .teamagent .claude
addopts = -m "not e2e"
markers =
    e2e: marks tests as end-to-end requiring live services (run with 'pytest -m e2e')
